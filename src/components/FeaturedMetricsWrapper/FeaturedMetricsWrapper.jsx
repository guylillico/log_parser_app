import React from "react"
import { StyledMetricsWrapper } from "./FeaturedMetricsWrapper.styled"

const FeaturedMetricsWrapper = ({ children }) => {
  return <StyledMetricsWrapper>{children}</StyledMetricsWrapper>
}

export default FeaturedMetricsWrapper
